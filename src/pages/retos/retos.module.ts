import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RetosPage } from './retos';

@NgModule({
  declarations: [
    RetosPage,
  ],
  imports: [
    IonicPageModule.forChild(RetosPage),
  ],
})
export class RetosPageModule {}
